-- CREATE DATABASE recipes_app;

USE recipes_app;

-- CREATE TABLE allergens_ingredients(
--     name VARCHAR(255),
--     ingredient VARCHAR(255)
-- );

-- INSERT INTO allergens_ingredients VALUES("gluten", "Beer")

-- SELECT ingredient FROM allergens_ingredients WHERE name = 'dairy'
